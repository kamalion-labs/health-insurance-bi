"use client";

import { usePage } from "@/hooks";

export default function CovidDashboard() {
  usePage({
    id: "covidDashboard",
    parentId: "covid",
    title: "Dashboard Covid-19",
  });

  return <></>;
}
